{% extends 'base.html.twig' %}

{% block body %}
    <!-- Crea la vista para mostrar todas las localidades.
    Usa la función indexAction($request) y la variable $localidades del archivo
    LocalidadController.php para mostrar todas las localidades. -->

    <h1>Localidades</h1>
    
    <!-- Botón Crear nueva entrada -->
    <a href="{{ path('localidad_new') }}" class="btn btn-success">
        <span class="glyphicon glyphicon-plus"></span> Crear nueva entrada
    </a>
    <br/>
    <br/>

    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Código postal</th>
            </tr>
        </thead>
        <tbody>
        {% for localidad in localidades %}
            <tr>
                <td>{{ localidad.nombre }}</td>
                <td>{{ localidad.codigopostal }}</td>
                <!-- Botón Editar -->
                <td>
                    <a href="{{ path('localidad_edit', { 'id': localidad.id }) }}" class="btn btn-primary">
                        <span class="glyphicon glyphicon-pencil"></span> Editar
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
