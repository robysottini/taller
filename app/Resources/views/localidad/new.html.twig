{% extends 'base.html.twig' %}

{% block body %}
    <h1>Crear localidad</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('localidad_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
