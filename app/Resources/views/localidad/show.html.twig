{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-10">
            <h1>Localidad. <small>La nueva localidad fue creada con éxito.</small>
        </div>
        <div class="col-md-2">
            <a href="{{ path('localidad_index') }}" class="btn btn-primary pull-right">
                <span class="glyphicon glyphicon-list"></span> Volver a la lista
            </a>
        </div>       
    </div>

    <table class="table table-striped table-bordered table-hover">
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ localidad.nombre }}</td>
            </tr>
            <tr>
                <th>Código postal</th>
                <td>{{ localidad.codigopostal }}</td>
            </tr>
        </tbody>
    </table>
    
    <!-- Botón Editar -->
    <a href="{{ path('localidad_edit', { 'id': localidad.id }) }}" class="btn btn-primary pull-right">
        <span class="glyphicon glyphicon-pencil"></span> Editar
    </a>

    {{ form_start(delete_form) }}
        <button type="submit" class="btn btn-danger">
            <span class="glyphicon glyphicon-trash"></span> Borrar
        </button>
    {{ form_end(delete_form) }}
{% endblock %}
